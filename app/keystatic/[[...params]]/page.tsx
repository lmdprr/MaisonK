/**
 * Route attrape-tout de l'admin (`/keystatic`, `/keystatic/collection/...`).
 *
 * Le rendu est assuré par `app/keystatic/layout.tsx` ; cette page n'existe que
 * pour que l'App Router reconnaisse l'URL.
 */
export default function KeystaticPage() {
  return null
}
